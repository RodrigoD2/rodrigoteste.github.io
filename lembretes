<nav> = para navegação
<ul> = para listas
<li> = para listas
<a> = para links
<a href="#contatos">Contatos</a> exemplo para links (#)usado para links internos
<br> = usado para quebrar textos em pontos determinados. ex:<h1>Desenvolvedor <br>Front End & <br>UX/UI Designer</h1>
<strong><strong/> = serve para marcar uma determinda palavra, frase ou texto, para poder ser modificada
id="" = serve para criar links internos no site
class="" = serve para colocar classe em um determinado elemento para melhor estiliza lo

padding = distancia interna do elemento
margin = distância externa do elemento em relção as bordas, topo e botton
font-family = seliciona o tipo de fonte a ser usada
color = muda a cor do elemento
list-style = remove decoração padrão do ul
max-width: 100%; = usado para controlar o tamanho do conteudo
display: block; usado para não deixar o conteudo estourar, tipo img.
display: flex; = usado para organizar os itens um ao lado do outro
justify-content: space-between; = usado justificar a posição dos elementos no bloco
align-items: center; = usado para alinhar os lementos no bloco
gap: 10px; = usado para distanciar um elemento do outro
flex-wrap: wrap; = usado para quebrar o conteudo quando for diminuindo
font-size: 1.5rem; = usado para determinar o tamanho da font
line-height: 1.3; = usado para determniar a altura da linha
rem = unidade de medida, 1rem equivale a 16 px, 2 rem = 32
text-decoration: none; = usado para remover o sublinhado de links
flex-direction: column; = usado para direcionar os intens no flex, neste caso colunas
@media (max-width: 950px) = usado para definir break-points, define um novo posicionamento do site a partir de determinado tamnho em px
background: #f5f5f5; = usado para definir a cor de fundo de um elemento
border-radius: 4px; = coloca bordas em um elemento
grid-template-columns: 4fr 8fr;  = usado para definir a quantidade de colunas do site. Ex: 4fr= 4 colunas de grid, 2fr= 2 colunas de grid, 8fr, 8 colunas de grid
display: grid; = usado para determinar o tipo de grid do layout do site 
::before { content: "";} = usado para adicionar itens ao lado do elemento. ex:::before { content: "item";}
z-index: -1; = usado para por uma imagem ou elemento para traz ou para frente. Ex: -1 -2 -3, joga o elemento para traz, 1, 2, 3, joga o elemento para frente
position: absolute; position: relative; = usado para determinar o posicionamento do conteúdo em relação a tela
top, botton, left, rigth =  usado para determinar a posição do elemento ou conteúdo em determinada posição. Ex: cima, baixo, esqueda, direita
word-break: break-all; = usado para quebrar uma determinada palavra
text-transform: uppercase; = usado para modificar as letras de um determiadado texto. Ex: upercase, deixa tudo maiusculo
flex: 1; = usado para colocar os container dos itens do mesmo tamanho

5:36 tempo que parei da aula